# if user not define domian, use default
domain: domain

# each domain.xml these elements will be replace by default
common:
        - name
        - os
        - memory
        - serial
        - interface
        - active
        - emulator

# individual choic
replaceable:
        - cpu
        - seclabel
        - disk
        - devices
        - clock
